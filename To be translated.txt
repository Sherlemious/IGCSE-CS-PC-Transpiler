INPUT X
IF X >= 0 THEN
    PRINT "POSITIVE OR ZERO"
    IF X = 0 THEN
        PRINT "EQUAL TO ZERO"
        ELSE
        IF X > 0 THEN
            PRINT "POSITIVE"
        ENDIF
    ENDIF
ELSE
PRINT "Negative"
ENDIF